# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build workflow_automation`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t workflow_automation`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system workflow_automation && \
          adduser --system -G workflow_automation workflow_automation

COPY dist/apps/workflow_automation workflow_automation/
RUN chown -R workflow_automation:workflow_automation .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix workflow_automation --omit=dev -f install

CMD [ "node", "workflow_automation" ]
